export const PATH = Object.freeze({
  root: '/',
  signIn: '/signin',
  signUp: '/signup',
  todo: '/todo',
});

export const API_PATH = Object.freeze({
  root: '/',
  signIn: '/signin',
  signUp: '/signup',
  todos: '/todos',
  auth: '/auth',
});
