export const PATH_ROUTE = Object.freeze({
  root: '/',
  signIn: '/signin',
  signUp: '/signup',
  todo: '/todo',
});

export const PATH_API = Object.freeze({
  root: '/',
  signIn: '/signin',
  signUp: '/signup',
  todos: '/todos',
  auth: '/auth',
});
